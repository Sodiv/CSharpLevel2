<Window x:Class="ListEmployee.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListEmployee"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Список сотрудников" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="0" Grid.Row="0"/>
        <TextBlock Text="Список департаментов" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1" Grid.Row="0"/>        
        <ListBox ItemsSource="{Binding employeeViewModels, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedEmployee}" Grid.Column="0" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock FontSize="18" Text="{Binding Model.Name}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Возраст: "/>
                            <TextBlock Text="{Binding Model.Age}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Отдел: "/>
                            <ComboBox ItemsSource="{Binding departmentViewModels}"
                                  DisplayMemberPath="Model.Name"
                                  SelectedValue="{Binding Model.DepartmentId}"
                                  SelectedValuePath="Model.ID" IsEnabled="False"/>
                        </StackPanel>
                        
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Column="1" Grid.Row="1" DataContext="{Binding SelectedEmployee}">
            <TextBlock Text="Имя:" FontWeight="Bold"/>
            <TextBox Text="{Binding Model.Name}"/>
            <TextBlock Text="Возраст:" FontWeight="Bold"/>
            <TextBox Text="{Binding Model.Age}"/>
            <TextBlock Text="Отдел:" FontWeight="Bold"/>
            <ComboBox ItemsSource="{Binding departmentViewModels}"
                                  DisplayMemberPath="Model.Name"
                                  SelectedValue="{Binding Model.DepartmentId}"
                                  SelectedValuePath="Model.ID"/>
        </StackPanel>
        <StackPanel Grid.Column="0" Grid.Row="2" Orientation="Horizontal">
            <Button Click="Button_Click">Добавить Сотрудника</Button>
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal">
            <Button Click="Button_Click_1">Добавить отдел</Button>
        </StackPanel>
    </Grid>
</Window>
