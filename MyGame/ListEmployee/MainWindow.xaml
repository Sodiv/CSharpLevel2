<Window x:Class="ListEmployee.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListEmployee"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Список сотрудников" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="0" Grid.Row="0"/>
        <TextBlock Text="Список департаментов" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1" Grid.Row="0"/>
        <DataGrid x:Name="employeeList" ItemsSource="{Binding Employees}" Grid.Column="0" Grid.Row="1" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="Имя" Width="150"/>
                <DataGridTextColumn Binding="{Binding Age}" Header="Возраст"/>
                <DataGridTextColumn Binding="{Binding Department.Name}" Header="Отдел" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid ItemsSource="{Binding Departments}" Grid.Column="1" Grid.Row="1" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Name}" Header="Отдел"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Column="0" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Command="{Binding AddCommand}" Margin="5" Background="#FF28B921">Добавить Сотрудника</Button>
            <Button Command="{Binding EditCommand}" CommandParameter="{Binding ElementName=employeeList, Path=SelectedItem}">
                Изменить сотрудника
            </Button>
            <Button Command="{Binding DeleteCommand}"
                    CommandParameter="{Binding ElementName=employeeList, Path=SelectedItem}"
                    Margin="5" Background="#FFB95821">Удалить сотрудника</Button>
        </StackPanel>        
        <StackPanel Grid.Column="1" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Margin="5" Command="{Binding AddDepCommand}">Добавить отдел</Button>
            <Button Margin="5" Command="{Binding RemoveDepCommand}"
                    CommandParameter="{Binding SelectedDepartment}">Удалить отдел</Button>
        </StackPanel>
    </Grid>
</Window>
